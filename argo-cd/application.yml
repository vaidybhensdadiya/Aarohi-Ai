apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: aarohi-ai-app
  namespace: argocd
  metadata:
  annotations:
    # Track the repository
    argocd-image-updater.argoproj.io/image-list: webapp=coder122/aarohi-ai-web-app
    
    # Map the new tag data directly to your Helm parameters
    argocd-image-updater.argoproj.io/webapp.helm.image-name: webApp.image.repository
    argocd-image-updater.argoproj.io/webapp.helm.image-tag: webApp.image.tag
    
    # Strategy parameters
    argocd-image-updater.argoproj.io/update-strategy: latest
    
    # EXCLUSIVELY USE GIT WRITE-BACK (Fixed here)
    argocd-image-updater.argoproj.io/write-back-method: git
    
    # Target branch for the automated commit
    argocd-image-updater.argoproj.io/git-branch: KrishV


spec:
  project: default

  source:
    repoURL: https://github.com/vaidybhensdadiya/Aarohi-Ai.git
    targetRevision: KrishV
    path: aarohi-ai
    helm:
      valueFiles:
        - values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: aarohi-ai

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true


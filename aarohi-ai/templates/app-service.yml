apiVersion: v1
kind: Service
metadata:
  name: aarohi-app-service
  namespace: {{ .Values.global.namespace }}
spec:
  type: NodePort
  selector:
    app: aarohi-web
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 32000


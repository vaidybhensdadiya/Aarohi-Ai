apiVersion: v1
kind: Service
metadata:
  name: aarohi-app-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: aarohi-web
spec:
  type: NodePort
  selector:
    app: aarohi-web
  ports:
  - name: http
    protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 32000

